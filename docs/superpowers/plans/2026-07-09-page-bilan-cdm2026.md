# Page bilan CDM 2026 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer l'email quotidien MPP par une page web statique unique, vivante et interactive (classement, graphes d'évolution, exploration des pronostics), hébergeable sur Cloudflare Pages.

**Architecture:** Une couche de données JSON versionnée (`data/`) alimentée en delta par des appels HTTP directs à l'API MPP (`src/fetch.mjs`). Toute la logique de calcul (rangs, deltas, stats pronos) vit dans un module pur testable (`src/lib/compute.mjs`), partagé par le fetch et par le front. Un script de build (`src/build.mjs`) injecte données + CSS + JS + ECharts dans `src/template.html` et produit un unique `dist/index.html` auto-suffisant, commité et déployé sur Cloudflare Pages.

**Tech Stack:** Node 24 (ESM, `fetch` natif, `node --test`), JavaScript vanilla côté front, ECharts (inliné au build, aucune dépendance CDN au runtime).

> **Projet cible : `mppreporting`** (repo courant). Le projet `/home/willy/workspace/wc2026`
> est **uniquement une source d'inspiration et de données** (générateur d'email d'origine,
> `historique_points.json`, config joueurs) — on n'y touche pas. Les données `data/players.json`
> et `data/history.json` en sont dérivées et déjà présentes dans mppreporting (Task 1 faite par
> copie). Les liens relatifs de ce plan/spec vers des fichiers wc2026 pointent donc vers le repo
> d'inspiration, pas vers mppreporting.

## Global Constraints

- **Runtime page** : `dist/index.html` doit être **auto-suffisant** — aucune requête réseau externe au chargement (données, CSS, JS et ECharts tous inline).
- **Node ≥ 24**, modules **ESM** (`.mjs`), aucune dépendance runtime ; seule devDependency autorisée : `echarts` (inliné au build).
- **Tests** : `node --test` + `node:assert/strict`, fichiers `*.test.mjs`. Toute logique de calcul est testée ; le visuel est vérifié en ouvrant `dist/index.html`.
- **Données versionnées** : `data/*.json` et `dist/index.html` sont commités. Le token MPP ne l'est **jamais** (`.mpp-token` gitignoré).
- **Challenge MPP** : `mpp_challenge_UDMSMC8T`. Host API : `https://api.mpp.football`.
- **16 joueurs**, identifiés par `uid` (`user_XXXXXXX`). Correspondance uid → prénom/pseudo/couleur : voir Task 1.
- **Commits** : messages en français, **sans** trailer `Co-Authored-By`.
- **Ton éditorial du bilan** : décontracté, chambreur, vocabulaire football (repris de l'existant).

---

## File Structure

| Fichier | Responsabilité |
|---|---|
| `package.json` | Config Node (type: module), scripts `fetch`/`build`/`test`, devDep `echarts` |
| `data/players.json` | 16 joueurs : uid, name, pseudo, color (palette adaptée fond sombre) |
| `data/history.json` | Points cumulés par journée (repris de `historique_points.json`) |
| `data/matches.json` | Matchs : id, phase, date, équipes, drapeaux, score réel, status |
| `data/forecasts.json` | Pronos par joueur × match : score, points, result |
| `src/lib/compute.mjs` | **Logique pure** : rangs, deltas, séries graphes, statut prono, stats joueur, merge delta |
| `src/lib/compute.test.mjs` | Tests de `compute.mjs` |
| `src/fetch.mjs` | Récupère standings + matchs + pronos via HTTP, merge en delta dans `data/` |
| `src/fetch.test.mjs` | Tests de la logique de merge/normalisation du fetch |
| `src/template.html` | Squelette HTML + CSS (thème stade nocturne), placeholders d'injection |
| `src/app.js` | Front : onglets, graphes ECharts, filtres, rendu des 3 vues pronos |
| `src/build.mjs` | Assemble `dist/index.html` (injecte data + app.js + CSS + ECharts) |
| `src/build.test.mjs` | Tests de l'assemblage (présence des marqueurs, données injectées) |
| `dist/index.html` | **Livrable** déployé sur Cloudflare Pages |
| `CLAUDE.md` | Mis à jour : nouveau workflow fetch → build → déploiement |

**Note d'affinage vs spec** : ECharts est inliné **depuis `node_modules/echarts/dist/echarts.min.js`** au build (devDependency), plutôt qu'une copie manuelle dans `vendor/`. Un fichier de moins à gérer, même résultat (aucune dépendance runtime).

---

## Task 1: Scaffold du projet + bootstrap des données statiques

**Files:**
- Create: `package.json`, `.gitignore` (append), `data/players.json`, `data/history.json`
- Modify: `.gitignore`

**Interfaces:**
- Produces: `data/players.json` = `Array<{uid:string, name:string, pseudo:string, color:string}>` ; `data/history.json` = `{ [label:string]: { [uid:string]: number } }`.

- [ ] **Step 1: Initialiser package.json**

Create `package.json` :
```json
{
  "name": "wc2026-bilan",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "fetch": "node src/fetch.mjs",
    "build": "node src/build.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "echarts": "^5.5.0"
  }
}
```

- [ ] **Step 2: Installer echarts (devDependency)**

Run: `cd /home/willy/workspace/mppreporting && npm install`
Expected: `node_modules/echarts/dist/echarts.min.js` existe.
Verify: `test -f node_modules/echarts/dist/echarts.min.js && echo OK`

- [ ] **Step 3: Ignorer node_modules et le token**

Append to `.gitignore` :
```
# Node
node_modules/

# Token MPP — ne jamais versionner
.mpp-token
```

- [ ] **Step 4: Créer data/players.json**

Palette adaptée au fond sombre (certaines couleurs de l'email étaient trop foncées ; Vincent avait la même que Christophe → corrigé). Create `data/players.json` :
```json
[
  { "uid": "user_10698233", "name": "JC",         "pseudo": "JCBBBB",          "color": "#ff5a5f" },
  { "uid": "user_8701920",  "name": "Christophe", "pseudo": "ChrisCouder",     "color": "#5aa9e6" },
  { "uid": "user_8702965",  "name": "Eric",       "pseudo": "ZINEDINE_RICOU",  "color": "#2ec4b6" },
  { "uid": "user_8616704",  "name": "Marion",     "pseudo": "Go_3eme_Etoile",  "color": "#ffd166" },
  { "uid": "user_8422848",  "name": "Willy",      "pseudo": "willy3cub_",      "color": "#f4a261" },
  { "uid": "user_8620122",  "name": "Soizic",     "pseudo": "Soiiiiiiz",       "color": "#a8dadc" },
  { "uid": "user_10189113", "name": "Mathilde",   "pseudo": "MathouDori",      "color": "#52b788" },
  { "uid": "user_8619563",  "name": "Clément",    "pseudo": "Coach-33",        "color": "#c77dff" },
  { "uid": "user_8736186",  "name": "Stéphen",    "pseudo": "THE_GOAT_STEPH",  "color": "#b8c0ff" },
  { "uid": "user_9239662",  "name": "Manu",       "pseudo": "GOAT#UE2XW4MC",   "color": "#ced4da" },
  { "uid": "user_8619031",  "name": "Kawett",     "pseudo": "kawett",          "color": "#fb8500" },
  { "uid": "user_10595758", "name": "Candy",      "pseudo": "DidiiDeschamps",  "color": "#ff5da2" },
  { "uid": "user_8620746",  "name": "Célia",      "pseudo": "ORYDERAIMBAULT",  "color": "#e0955f" },
  { "uid": "user_8745208",  "name": "Vincent",    "pseudo": "Easybet",         "color": "#48cae4" },
  { "uid": "user_8742869",  "name": "Ben",        "pseudo": "Zhufazan",        "color": "#a7c957" },
  { "uid": "user_8738144",  "name": "Tonton",     "pseudo": "Tonton-du-Plaid", "color": "#90e0ef" }
]
```

- [ ] **Step 5: Créer data/history.json depuis l'existant**

Run: `cd /home/willy/workspace/mppreporting && cp historique_points.json data/history.json && echo OK`
Expected: `data/history.json` contient les clés J1…J22.
Verify: `node -e "const h=require('./data/history.json'); console.log(Object.keys(h).length, 'journées')"`
Expected: `20 journées` (J1, J2…J6, J7-9, J10-11, J12…J22).

- [ ] **Step 6: Commit**

```bash
git add package.json .gitignore data/players.json data/history.json
git commit -m "Scaffold projet + bootstrap données joueurs et historique"
```

---

## Task 2: Module compute — rangs et deltas

**Files:**
- Create: `src/lib/compute.mjs`, `src/lib/compute.test.mjs`

**Interfaces:**
- Produces:
  - `rankStandings(points)` → `Array<{uid, pts, rank}>` trié pts desc, rang 1-indexé (égalité : ordre stable, même rang non géré → rang par position).
  - `ranksOf(points)` → `{ [uid]: rank }`.
  - `computeDeltas(current, previous)` → `{ [uid]: { pts, dp, rankNow, rankPrev, dr } }` où `dp = pts - ptsPrev`, `dr = rankPrev - rankNow` (positif = monte).
  - `points` / `current` / `previous` sont `{ [uid]: number }`.

- [ ] **Step 1: Écrire les tests (échouent)**

Create `src/lib/compute.test.mjs` :
```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { rankStandings, ranksOf, computeDeltas } from './compute.mjs';

test('rankStandings trie par points décroissants et numérote', () => {
  const out = rankStandings({ a: 100, b: 300, c: 200 });
  assert.deepEqual(out, [
    { uid: 'b', pts: 300, rank: 1 },
    { uid: 'c', pts: 200, rank: 2 },
    { uid: 'a', pts: 100, rank: 3 },
  ]);
});

test('ranksOf renvoie un rang par uid', () => {
  assert.deepEqual(ranksOf({ a: 100, b: 300, c: 200 }), { b: 1, c: 2, a: 3 });
});

test('computeDeltas calcule dp et dr (dr>0 = monte)', () => {
  const prev = { a: 100, b: 300, c: 200 }; // rangs: b1 c2 a3
  const cur  = { a: 400, b: 320, c: 210 }; // rangs: a1 b2 c3
  const d = computeDeltas(cur, prev);
  assert.equal(d.a.dp, 300);
  assert.equal(d.a.rankPrev, 3);
  assert.equal(d.a.rankNow, 1);
  assert.equal(d.a.dr, 2);      // 3 -> 1 : +2 places
  assert.equal(d.b.dr, -1);     // 1 -> 2 : -1 place
});
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec**

Run: `cd /home/willy/workspace/mppreporting && node --test src/lib/compute.test.mjs`
Expected: FAIL — `Cannot find module './compute.mjs'`.

- [ ] **Step 3: Implémenter compute.mjs (minimal)**

Create `src/lib/compute.mjs` :
```js
export function rankStandings(points) {
  return Object.entries(points)
    .sort((a, b) => b[1] - a[1])
    .map(([uid, pts], i) => ({ uid, pts, rank: i + 1 }));
}

export function ranksOf(points) {
  const out = {};
  for (const { uid, rank } of rankStandings(points)) out[uid] = rank;
  return out;
}

export function computeDeltas(current, previous) {
  const rNow = ranksOf(current);
  const rPrev = ranksOf(previous);
  const out = {};
  for (const [uid, pts] of Object.entries(current)) {
    const rankNow = rNow[uid];
    const rankPrev = rPrev[uid] ?? rankNow;
    out[uid] = {
      pts,
      dp: pts - (previous[uid] ?? 0),
      rankNow,
      rankPrev,
      dr: rankPrev - rankNow,
    };
  }
  return out;
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `cd /home/willy/workspace/mppreporting && node --test src/lib/compute.test.mjs`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/compute.mjs src/lib/compute.test.mjs
git commit -m "compute: rangs et deltas de classement"
```

---

## Task 3: Module compute — séries pour les graphes

**Files:**
- Modify: `src/lib/compute.mjs`, `src/lib/compute.test.mjs`

**Interfaces:**
- Consumes: `rankStandings` (Task 2).
- Produces:
  - `labelsOf(history)` → `string[]` (clés dans l'ordre d'insertion).
  - `seriesPoints(history, uids)` → `{ [uid]: (number|null)[] }` aligné sur `labelsOf` (null si absent d'une journée).
  - `seriesRanks(history, uids)` → `{ [uid]: (number|null)[] }` rang par journée (1 = 1er).

- [ ] **Step 1: Ajouter les tests (échouent)**

Append to `src/lib/compute.test.mjs` :
```js
import { labelsOf, seriesPoints, seriesRanks } from './compute.mjs';

const HIST = {
  J1: { a: 10, b: 20 },
  J2: { a: 50, b: 30 },
};

test('labelsOf conserve l’ordre', () => {
  assert.deepEqual(labelsOf(HIST), ['J1', 'J2']);
});

test('seriesPoints aligne les points par journée', () => {
  assert.deepEqual(seriesPoints(HIST, ['a', 'b']), { a: [10, 50], b: [20, 30] });
});

test('seriesRanks donne le rang par journée', () => {
  // J1: b(20)=1, a(10)=2 | J2: a(50)=1, b(30)=2
  assert.deepEqual(seriesRanks(HIST, ['a', 'b']), { a: [2, 1], b: [1, 2] });
});

test('seriesPoints met null si le joueur manque une journée', () => {
  const h = { J1: { a: 10 }, J2: { a: 20, b: 5 } };
  assert.deepEqual(seriesPoints(h, ['a', 'b']), { a: [10, 20], b: [null, 5] });
});
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cd /home/willy/workspace/mppreporting && node --test src/lib/compute.test.mjs`
Expected: FAIL — `labelsOf is not a function` (imports absents).

- [ ] **Step 3: Implémenter**

Append to `src/lib/compute.mjs` :
```js
export function labelsOf(history) {
  return Object.keys(history);
}

export function seriesPoints(history, uids) {
  const labels = labelsOf(history);
  const out = {};
  for (const uid of uids) {
    out[uid] = labels.map((l) => (uid in history[l] ? history[l][uid] : null));
  }
  return out;
}

export function seriesRanks(history, uids) {
  const labels = labelsOf(history);
  const rankByLabel = labels.map((l) => ranksOf(history[l]));
  const out = {};
  for (const uid of uids) {
    out[uid] = rankByLabel.map((r) => r[uid] ?? null);
  }
  return out;
}
```
(`ranksOf` est déjà défini dans le fichier — pas de réimport.)

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `cd /home/willy/workspace/mppreporting && node --test src/lib/compute.test.mjs`
Expected: PASS (7 tests au total).

- [ ] **Step 5: Commit**

```bash
git add src/lib/compute.mjs src/lib/compute.test.mjs
git commit -m "compute: séries points et rangs pour les graphes"
```

---

## Task 4: Module compute — statut des pronos et stats joueur

**Files:**
- Modify: `src/lib/compute.mjs`, `src/lib/compute.test.mjs`

**Interfaces:**
- Produces:
  - `forecastStatus(prono, match)` → `'exact' | 'result' | 'miss' | 'pending'`.
    `prono` = `{score1, score2}` (ou null) ; `match` = `{score1, score2, status}` (`status:'played'|'pending'`).
    `exact` si les deux scores égaux ; `result` si même issue (1/N/2) ; `miss` sinon ; `pending` si match non joué ou prono absent.
  - `outcome(s1, s2)` → `'1' | 'N' | '2'`.
  - `playerStats(uid, forecasts, matches)` → `{ played, exact, result, miss, exactRate, resultRate, points, ptsPerMatch, best, worst }` où `best`/`worst` = `{matchId, points}` sur matchs joués (ou null).
    `forecasts` = `{ [uid]: { [matchId]: {score1, score2, points, result} } }` ; `matches` = `Array<{id, score1, score2, status}>`.

- [ ] **Step 1: Ajouter les tests (échouent)**

Append to `src/lib/compute.test.mjs` :
```js
import { forecastStatus, outcome, playerStats } from './compute.mjs';

test('outcome donne l’issue', () => {
  assert.equal(outcome(2, 0), '1');
  assert.equal(outcome(1, 1), 'N');
  assert.equal(outcome(0, 3), '2');
});

test('forecastStatus: exact / result / miss / pending', () => {
  const m = { score1: 2, score2: 1, status: 'played' };
  assert.equal(forecastStatus({ score1: 2, score2: 1 }, m), 'exact');
  assert.equal(forecastStatus({ score1: 3, score2: 1 }, m), 'result');
  assert.equal(forecastStatus({ score1: 0, score2: 2 }, m), 'miss');
  assert.equal(forecastStatus({ score1: 2, score2: 1 }, { ...m, status: 'pending' }), 'pending');
  assert.equal(forecastStatus(null, m), 'pending');
});

test('playerStats agrège les pronos joués', () => {
  const matches = [
    { id: 'm1', score1: 2, score2: 1, status: 'played' },
    { id: 'm2', score1: 0, score2: 0, status: 'played' },
    { id: 'm3', score1: 1, score2: 1, status: 'pending' },
  ];
  const forecasts = { u: {
    m1: { score1: 2, score2: 1, points: 24 }, // exact
    m2: { score1: 1, score2: 2, points: 0 },  // miss
    m3: { score1: 1, score2: 1, points: 0 },  // pending (match non joué)
  } };
  const s = playerStats('u', forecasts, matches);
  assert.equal(s.played, 2);
  assert.equal(s.exact, 1);
  assert.equal(s.miss, 1);
  assert.equal(s.points, 24);
  assert.equal(s.best.matchId, 'm1');
  assert.equal(s.exactRate, 0.5);
});
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cd /home/willy/workspace/mppreporting && node --test src/lib/compute.test.mjs`
Expected: FAIL — `outcome is not a function`.

- [ ] **Step 3: Implémenter**

Append to `src/lib/compute.mjs` :
```js
export function outcome(s1, s2) {
  if (s1 > s2) return '1';
  if (s1 < s2) return '2';
  return 'N';
}

export function forecastStatus(prono, match) {
  if (!prono || !match || match.status !== 'played') return 'pending';
  if (prono.score1 === match.score1 && prono.score2 === match.score2) return 'exact';
  if (outcome(prono.score1, prono.score2) === outcome(match.score1, match.score2)) return 'result';
  return 'miss';
}

export function playerStats(uid, forecasts, matches) {
  const pf = forecasts[uid] || {};
  let played = 0, exact = 0, result = 0, miss = 0, points = 0;
  let best = null, worst = null;
  for (const m of matches) {
    const prono = pf[m.id];
    const st = forecastStatus(prono, m);
    if (st === 'pending') continue;
    played++;
    if (st === 'exact') exact++;
    else if (st === 'result') result++;
    else miss++;
    const pts = prono?.points ?? 0;
    points += pts;
    if (!best || pts > best.points) best = { matchId: m.id, points: pts };
    if (!worst || pts < worst.points) worst = { matchId: m.id, points: pts };
  }
  return {
    played, exact, result, miss, points,
    exactRate: played ? exact / played : 0,
    resultRate: played ? (exact + result) / played : 0,
    ptsPerMatch: played ? points / played : 0,
    best, worst,
  };
}
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `cd /home/willy/workspace/mppreporting && node --test src/lib/compute.test.mjs`
Expected: PASS (10 tests au total).

- [ ] **Step 5: Commit**

```bash
git add src/lib/compute.mjs src/lib/compute.test.mjs
git commit -m "compute: statut des pronos et stats par joueur"
```

---

## Task 5: Fixtures matches.json et forecasts.json (permet de bâtir le front sans l'API)

**Files:**
- Create: `data/matches.json`, `data/forecasts.json`

**Interfaces:**
- Produces: fichiers au format défini en Task 4 (`matches` = `Array<{id, phase, date, team1, flag1, team2, flag2, score1, score2, status}>` ; `forecasts` = `{ [uid]: { [matchId]: {score1, score2, points, result} } }`). Données réalistes provisoires, remplacées par le fetch réel en Task 12.

- [ ] **Step 1: Créer data/matches.json (échantillon représentatif)**

Create `data/matches.json` (2 matchs joués + 1 à venir, phases variées) :
```json
[
  { "id": "m_arg_egy", "phase": "1/8", "date": "2026-07-07T18:00:00Z", "team1": "Argentine", "flag1": "🇦🇷", "team2": "Égypte",   "score1": 3, "score2": 2, "status": "played" },
  { "id": "m_sui_col", "phase": "1/8", "date": "2026-07-07T22:00:00Z", "team1": "Suisse",    "flag1": "🇨🇭", "team2": "Colombie", "score1": 0, "score2": 0, "status": "played" },
  { "id": "m_fra_mar", "phase": "1/4", "date": "2026-07-10T22:00:00Z", "team1": "France",    "flag1": "🇫🇷", "team2": "Maroc",    "score1": null, "score2": null, "status": "pending" }
]
```

- [ ] **Step 2: Créer data/forecasts.json (2 joueurs, pour tester les 3 vues)**

Create `data/forecasts.json` :
```json
{
  "user_10698233": {
    "m_arg_egy": { "score1": 2, "score2": 2, "points": 16, "result": "result" },
    "m_sui_col": { "score1": 1, "score2": 0, "points": 16, "result": "result" },
    "m_fra_mar": { "score1": 2, "score2": 0, "points": 0,  "result": "pending" }
  },
  "user_8701920": {
    "m_arg_egy": { "score1": 3, "score2": 2, "points": 32, "result": "exact" },
    "m_sui_col": { "score1": 2, "score2": 1, "points": 0,  "result": "miss" },
    "m_fra_mar": { "score1": 1, "score2": 1, "points": 0,  "result": "pending" }
  }
}
```

- [ ] **Step 3: Vérifier la cohérence via compute**

Run:
```bash
cd /home/willy/workspace/mppreporting && node -e "
import('./src/lib/compute.mjs').then(async C => {
  const fs = await import('node:fs');
  const matches = JSON.parse(fs.readFileSync('data/matches.json'));
  const fc = JSON.parse(fs.readFileSync('data/forecasts.json'));
  console.log('JC', C.playerStats('user_10698233', fc, matches));
});
"
```
Expected: objet stats avec `played: 2` (le match `pending` est exclu).

- [ ] **Step 4: Commit**

```bash
git add data/matches.json data/forecasts.json
git commit -m "Fixtures matchs et pronos (provisoires, avant fetch API)"
```

---

## Task 6: Pipeline de build — index.html auto-suffisant

**Files:**
- Create: `src/template.html`, `src/app.js`, `src/build.mjs`, `src/build.test.mjs`

**Interfaces:**
- Consumes: `data/*.json`, `node_modules/echarts/dist/echarts.min.js`.
- Produces: `dist/index.html`. `build.mjs` exporte `buildHtml({players, history, matches, forecasts, echarts, appJs})` → `string` (HTML complet). `template.html` contient les marqueurs littéraux `/*__DATA__*/`, `/*__ECHARTS__*/`, `/*__APP__*/`. `app.js` expose `window.__WC` (les données) et une fonction `initApp()`.

- [ ] **Step 1: Créer le template minimal**

Create `src/template.html` :
```html
<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>CDM 2026 — Bilan SHRS Football Club</title>
<style>
:root{--bg1:#0a0e27;--bg2:#141b3c;--gold:#FFD166;--ink:#eef1ff;--muted:#9aa3c7;}
*{box-sizing:border-box}
body{margin:0;background:linear-gradient(160deg,var(--bg1),var(--bg2));color:var(--ink);font-family:system-ui,Segoe UI,Roboto,Arial,sans-serif;}
#app{max-width:1100px;margin:0 auto;padding:20px;}
</style>
</head>
<body>
<div id="app">Chargement…</div>
<script>/*__ECHARTS__*/</script>
<script>window.__WC=/*__DATA__*/;</script>
<script>/*__APP__*/</script>
<script>initApp();</script>
</body>
</html>
```

- [ ] **Step 2: Créer un app.js minimal (rempli aux tasks suivantes)**

Create `src/app.js` :
```js
function initApp() {
  const app = document.getElementById('app');
  const { players, history } = window.__WC;
  app.textContent = `${players.length} joueurs • ${Object.keys(history).length} journées`;
}
```

- [ ] **Step 3: Écrire le test de build (échoue)**

Create `src/build.test.mjs` :
```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { buildHtml } from './build.mjs';

test('buildHtml injecte data, echarts et app, sans marqueurs restants', () => {
  const html = buildHtml({
    players: [{ uid: 'u', name: 'X', pseudo: 'x', color: '#fff' }],
    history: { J1: { u: 10 } },
    matches: [],
    forecasts: {},
    echarts: 'ECHARTS_STUB',
    appJs: 'function initApp(){}',
  });
  assert.match(html, /ECHARTS_STUB/);
  assert.match(html, /"name":"X"/);
  assert.match(html, /function initApp/);
  assert.doesNotMatch(html, /__DATA__|__ECHARTS__|__APP__/);
});
```

- [ ] **Step 4: Lancer, vérifier l'échec**

Run: `cd /home/willy/workspace/mppreporting && node --test src/build.test.mjs`
Expected: FAIL — `Cannot find module './build.mjs'`.

- [ ] **Step 5: Implémenter build.mjs**

Create `src/build.mjs` :
```js
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..');
const read = (p) => readFileSync(join(ROOT, p), 'utf8');
const readJson = (p) => JSON.parse(read(p));

export function buildHtml({ players, history, matches, forecasts, echarts, appJs }) {
  const data = JSON.stringify({ players, history, matches, forecasts });
  return read('src/template.html')
    .replace('/*__ECHARTS__*/', () => echarts)
    .replace('/*__DATA__*/', () => data)
    .replace('/*__APP__*/', () => appJs);
}

export function build() {
  const html = buildHtml({
    players: readJson('data/players.json'),
    history: readJson('data/history.json'),
    matches: readJson('data/matches.json'),
    forecasts: readJson('data/forecasts.json'),
    echarts: read('node_modules/echarts/dist/echarts.min.js'),
    appJs: read('src/app.js'),
  });
  mkdirSync(join(ROOT, 'dist'), { recursive: true });
  writeFileSync(join(ROOT, 'dist/index.html'), html);
  console.log(`OK  dist/index.html  (${(html.length / 1024) | 0} Ko)`);
}

if (import.meta.url === `file://${process.argv[1]}`) build();
```
(`.replace` avec une fonction de remplacement évite l'interprétation des motifs `$` dans les données.)

- [ ] **Step 6: Lancer le test, vérifier le succès**

Run: `cd /home/willy/workspace/mppreporting && node --test src/build.test.mjs`
Expected: PASS.

- [ ] **Step 7: Générer le vrai build et le vérifier**

Run: `cd /home/willy/workspace/mppreporting && npm run build`
Expected: `OK  dist/index.html  (~NNNN Ko)`.
Verify (ouvrir dans un navigateur) : `dist/index.html` affiche « 16 joueurs • 20 journées ».

- [ ] **Step 8: Commit**

```bash
git add src/template.html src/app.js src/build.mjs src/build.test.mjs dist/index.html
git commit -m "Build: assemblage index.html auto-suffisant (data + echarts + app inline)"
```

---

## Task 7: Coquille de page + thème stade nocturne (hero, nav, podium)

> **Note visuelle** : au début de cette task, invoquer les compétences **frontend-design** (direction esthétique, stade nocturne électrique) et **dataviz** (avant tout code de graphe aux tasks 8-9). Le code ci-dessous pose la structure ; le style pétillant (glows, dégradés, micro-animations) est peaufiné avec ces compétences. Vérification = ouverture de `dist/index.html`.

**Files:**
- Modify: `src/app.js`, `src/template.html`

**Interfaces:**
- Consumes: `window.__WC` (`players`, `history`).
- Produces: `initApp()` rend une structure `<header>` (titre + dernière MàJ), `<nav>` (ancres Classement / Graphe / Pronos / Bilan) et une section podium `#podium` (top 3 de la dernière journée). Expose `latestLabel()` et `latestPoints()` réutilisés ensuite.

- [ ] **Step 1: Réécrire app.js — structure + podium**

Replace `src/app.js` content (⚠️ `app.js` s'exécute en `<script>` classique — **pas** d'`import`/`export` ; les helpers de `compute.mjs` sont redéfinis localement, petit périmètre assumé) :
```js
function latestLabel() {
  const labels = Object.keys(window.__WC.history);
  return labels[labels.length - 1];
}
function latestPoints() {
  return window.__WC.history[latestLabel()];
}
function playerByUid(uid) {
  return window.__WC.players.find((p) => p.uid === uid);
}
function rankStandings(points) {
  return Object.entries(points).sort((a, b) => b[1] - a[1])
    .map(([uid, pts], i) => ({ uid, pts, rank: i + 1 }));
}

function renderHero(root) {
  const label = latestLabel();
  root.insertAdjacentHTML('beforeend', `
    <header class="hero">
      <div class="trophy">🏆</div>
      <h1>COUPE DU MONDE 2026</h1>
      <p class="sub">Bilan SHRS Football Club — Ligue UDMSMC8T</p>
      <p class="upd">Dernière mise à jour : ${label}</p>
    </header>
    <nav class="nav">
      <a href="#classement">Classement</a>
      <a href="#graphe">Évolution</a>
      <a href="#pronos">Pronostics</a>
      <a href="#bilan">Bilan</a>
    </nav>
    <section id="podium" class="podium"></section>`);
}

function renderPodium() {
  const top3 = rankStandings(latestPoints()).slice(0, 3);
  const medals = ['🥇', '🥈', '🥉'];
  const order = [1, 0, 2]; // 2e, 1er, 3e pour l'effet marches
  document.getElementById('podium').innerHTML = order.map((i) => {
    const s = top3[i]; if (!s) return '';
    const p = playerByUid(s.uid);
    return `<div class="step step-${s.rank}" style="--c:${p.color}">
      <div class="medal">${medals[s.rank - 1]}</div>
      <div class="pname">${p.name}</div>
      <div class="ppts">${s.pts.toLocaleString('fr-FR')} pts</div>
    </div>`;
  }).join('');
}

function initApp() {
  const app = document.getElementById('app');
  app.innerHTML = '';
  renderHero(app);
  renderPodium();
}
```
> `compute.mjs` sert aux tests Node ; sa logique (rangs, statut prono) est dupliquée en quelques helpers locaux dans `app.js`, qui s'exécute en `<script>` classique. Périmètre volontairement petit pour éviter un bundler.

- [ ] **Step 2: Ajouter le CSS du thème (hero, nav, podium)**

Add inside `<style>` in `src/template.html` (avant `</style>`) :
```css
.hero{text-align:center;padding:34px 12px 10px}
.hero .trophy{font-size:44px;filter:drop-shadow(0 0 14px var(--gold))}
.hero h1{margin:6px 0;font-size:30px;letter-spacing:2px;
  background:linear-gradient(90deg,#fff,var(--gold));-webkit-background-clip:text;background-clip:text;color:transparent}
.hero .sub{color:var(--muted);margin:2px}
.hero .upd{color:var(--gold);font-size:13px;letter-spacing:1px}
.nav{position:sticky;top:0;z-index:5;display:flex;gap:8px;justify-content:center;flex-wrap:wrap;
  padding:10px;background:rgba(10,14,39,.75);backdrop-filter:blur(8px);border-bottom:1px solid #ffffff14}
.nav a{color:var(--ink);text-decoration:none;font-size:14px;padding:6px 12px;border-radius:20px;border:1px solid #ffffff22}
.nav a:hover{background:#ffffff14;box-shadow:0 0 12px #ffffff22}
.podium{display:flex;align-items:flex-end;justify-content:center;gap:14px;margin:26px 0}
.podium .step{text-align:center;background:#ffffff0d;border:1px solid var(--c);border-radius:12px;padding:14px 16px;min-width:96px;
  box-shadow:0 0 18px color-mix(in srgb,var(--c) 45%,transparent)}
.podium .step-1{padding-bottom:38px}
.podium .step-2{padding-bottom:22px}
.podium .medal{font-size:30px}
.podium .pname{font-weight:700;color:var(--c)}
.podium .ppts{color:var(--muted);font-size:13px}
@keyframes pop{from{transform:translateY(10px);opacity:0}to{transform:none;opacity:1}}
.podium .step{animation:pop .5s ease both}
.podium .step-1{animation-delay:.15s}
```

- [ ] **Step 3: Rebuild et vérifier**

Run: `cd /home/willy/workspace/mppreporting && npm run build`
Verify (navigateur) : hero avec trophée doré, nav sticky, podium 3 marches (Christophe 1er 4095 pts d'après `data/history.json` J22), animation d'apparition.

- [ ] **Step 4: Commit**

```bash
git add src/app.js src/template.html dist/index.html
git commit -m "Page: hero, navigation sticky et podium animé (thème stade nocturne)"
```

---

## Task 8: Le Grand Graphe — bump chart + points cumulés (ECharts)

> Invoquer **dataviz** avant de coder les options ECharts (échelles, couleurs sur fond sombre, tooltips lisibles).

**Files:**
- Modify: `src/app.js`, `src/template.html`

**Interfaces:**
- Consumes: `window.__WC.history`, `window.__WC.players`, ECharts global (`window.echarts`).
- Produces: section `#graphe` avec onglets `Bump (rangs)` / `Points cumulés`, un conteneur `#chart`, une instance ECharts. Fonctions `chartOptionRanks()` et `chartOptionPoints()` renvoyant des options ECharts. Légende ECharts native (clic = toggle série).

- [ ] **Step 1: Ajouter la section graphe dans initApp**

In `src/app.js`, add after `renderPodium();` in `initApp` :
```js
  document.getElementById('app').insertAdjacentHTML('beforeend', `
    <section id="graphe" class="card">
      <h2>📈 Évolution</h2>
      <div class="tabs">
        <button class="tab active" data-mode="ranks">Rangs</button>
        <button class="tab" data-mode="points">Points</button>
      </div>
      <div id="chart" style="height:460px"></div>
    </section>`);
  initChart();
```

- [ ] **Step 2: Implémenter les options + l'init du chart**

Append to `src/app.js` :
```js
function seriesFor(mode) {
  const { history, players } = window.__WC;
  const labels = Object.keys(history);
  const rankByLabel = labels.map((l) => {
    const r = {}; rankStandings(history[l]).forEach((s) => (r[s.uid] = s.rank)); return r;
  });
  return players.map((p) => ({
    name: p.name,
    type: 'line',
    smooth: true,
    symbol: 'circle',
    symbolSize: 6,
    lineStyle: { width: 2, color: p.color },
    itemStyle: { color: p.color },
    emphasis: { focus: 'series', lineStyle: { width: 4 } },
    data: labels.map((l, i) =>
      mode === 'ranks' ? (rankByLabel[i][p.uid] ?? null) : (history[l][p.uid] ?? null)),
  }));
}

function baseOption(mode) {
  const labels = Object.keys(window.__WC.history);
  return {
    backgroundColor: 'transparent',
    textStyle: { color: '#cfd6f5' },
    tooltip: { trigger: 'axis', order: mode === 'ranks' ? 'valueAsc' : 'valueDesc',
      backgroundColor: '#141b3c', borderColor: '#ffffff33', textStyle: { color: '#fff' } },
    legend: { type: 'scroll', top: 0, textStyle: { color: '#cfd6f5' }, inactiveColor: '#555' },
    grid: { left: 44, right: 20, top: 40, bottom: 60 },
    xAxis: { type: 'category', data: labels, axisLine: { lineStyle: { color: '#ffffff33' } } },
    yAxis: mode === 'ranks'
      ? { type: 'value', inverse: true, min: 1, max: 16, interval: 1, axisLabel: { formatter: '{value}e' }, splitLine: { lineStyle: { color: '#ffffff12' } } }
      : { type: 'value', splitLine: { lineStyle: { color: '#ffffff12' } } },
    dataZoom: [{ type: 'inside' }, { type: 'slider', bottom: 16, height: 18 }],
    series: seriesFor(mode),
    animationDuration: 700,
  };
}
const chartOptionRanks = () => baseOption('ranks');
const chartOptionPoints = () => baseOption('points');

let _chart;
function initChart() {
  _chart = window.echarts.init(document.getElementById('chart'), null, { renderer: 'canvas' });
  _chart.setOption(chartOptionRanks());
  document.querySelectorAll('.tab').forEach((btn) => btn.addEventListener('click', () => {
    document.querySelectorAll('.tab').forEach((b) => b.classList.remove('active'));
    btn.classList.add('active');
    _chart.setOption(btn.dataset.mode === 'ranks' ? chartOptionRanks() : chartOptionPoints(), true);
  }));
  window.addEventListener('resize', () => _chart.resize());
}
```

- [ ] **Step 3: CSS des cartes et onglets**

Add in `<style>` :
```css
.card{background:#ffffff0a;border:1px solid #ffffff1a;border-radius:16px;padding:16px 18px;margin:22px 0;
  box-shadow:0 8px 30px #00000040}
.card h2{margin:0 0 12px;font-size:20px}
.tabs{display:flex;gap:8px;margin-bottom:10px}
.tab{background:#ffffff10;color:var(--ink);border:1px solid #ffffff22;border-radius:20px;padding:6px 14px;cursor:pointer}
.tab.active{background:var(--gold);color:#141b3c;font-weight:700}
```

- [ ] **Step 4: Rebuild et vérifier**

Run: `cd /home/willy/workspace/mppreporting && npm run build`
Verify (navigateur) : graphe des rangs (1er en haut), 16 courbes colorées, légende cliquable (clic masque/affiche une série), survol → tooltip trié, slider de zoom en bas, bascule Rangs/Points fonctionnelle.

- [ ] **Step 5: Commit**

```bash
git add src/app.js src/template.html dist/index.html
git commit -m "Graphe: bump chart des rangs + points cumulés (ECharts, légende, zoom)"
```

---

## Task 9: Curseur temporel + animation « replay »

**Files:**
- Modify: `src/app.js`, `src/template.html`

**Interfaces:**
- Consumes: `_chart`, `chartOptionRanks/Points`, labels de `history`.
- Produces: contrôles `#replay` (bouton ▶/⏸ + slider journée) sous le graphe. Fonction `applyUpTo(idx)` qui régénère les séries tronquées à `labels[0..idx]` ; boucle d'animation qui incrémente `idx`.

- [ ] **Step 1: Ajouter les contrôles dans la section graphe**

In `src/app.js`, modify the `#graphe` HTML (Step 1 of Task 8) to add before `</section>` :
```html
      <div id="replay" class="replay">
        <button id="playBtn" class="tab">▶︎ Rejouer</button>
        <input id="scrub" type="range" min="0" value="0" step="1">
        <span id="scrubLabel" class="muted"></span>
      </div>
```

- [ ] **Step 2: Implémenter la logique de replay**

Append to `src/app.js` :
```js
function currentMode() {
  return document.querySelector('.tab.active[data-mode]')?.dataset.mode || 'ranks';
}
function applyUpTo(idx) {
  const labels = Object.keys(window.__WC.history);
  const clip = idx + 1;
  const opt = currentMode() === 'ranks' ? chartOptionRanks() : chartOptionPoints();
  opt.xAxis.data = labels.slice(0, clip);
  opt.series = opt.series.map((s) => ({ ...s, data: s.data.slice(0, clip) }));
  opt.dataZoom = opt.dataZoom.map((z) => ({ ...z, start: 0, end: 100 }));
  _chart.setOption(opt, true);
  document.getElementById('scrubLabel').textContent = labels[idx];
}
function initReplay() {
  const labels = Object.keys(window.__WC.history);
  const scrub = document.getElementById('scrub');
  const btn = document.getElementById('playBtn');
  scrub.max = String(labels.length - 1);
  scrub.value = String(labels.length - 1);
  document.getElementById('scrubLabel').textContent = labels[labels.length - 1];
  scrub.addEventListener('input', () => { pause(); applyUpTo(+scrub.value); });
  let timer = null;
  function pause() { if (timer) { clearInterval(timer); timer = null; btn.textContent = '▶︎ Rejouer'; } }
  function play() {
    let i = +scrub.value >= labels.length - 1 ? 0 : +scrub.value;
    btn.textContent = '⏸ Pause';
    timer = setInterval(() => {
      applyUpTo(i); scrub.value = String(i);
      if (i >= labels.length - 1) { pause(); return; }
      i++;
    }, 700);
  }
  btn.addEventListener('click', () => (timer ? pause() : play()));
}
```
Then call `initReplay();` at the end of `initChart()` (after the resize listener).

- [ ] **Step 3: CSS des contrôles**

Add in `<style>` :
```css
.replay{display:flex;align-items:center;gap:12px;margin-top:12px}
.replay input[type=range]{flex:1;accent-color:var(--gold)}
.muted{color:var(--muted);font-size:13px;min-width:52px}
```

- [ ] **Step 4: Rebuild et vérifier**

Run: `cd /home/willy/workspace/mppreporting && npm run build`
Verify (navigateur) : bouton « Rejouer » anime le graphe journée par journée ; le slider déplace manuellement la journée affichée ; le replay respecte l'onglet actif (rangs ou points).

- [ ] **Step 5: Commit**

```bash
git add src/app.js src/template.html dist/index.html
git commit -m "Graphe: curseur temporel et animation replay journée par journée"
```

---

## Task 10: Section classement (tableau live avec deltas)

**Files:**
- Modify: `src/app.js`, `src/template.html`

**Interfaces:**
- Consumes: `history`, `players`, helpers `rankStandings`, `latestLabel`.
- Produces: section `#classement` — tableau rang / joueur / pseudo / évolution (flèches ▲▼) / +pts journée / total, calculé entre la dernière et l'avant-dernière journée.

- [ ] **Step 1: Implémenter le rendu du classement**

Append to `src/app.js` :
```js
function renderClassement() {
  const { history, players } = window.__WC;
  const labels = Object.keys(history);
  const cur = history[labels[labels.length - 1]];
  const prev = history[labels[labels.length - 2]] || cur;
  const rankPrev = {}; rankStandings(prev).forEach((s) => (rankPrev[s.uid] = s.rank));
  const rows = rankStandings(cur).map((s) => {
    const p = players.find((x) => x.uid === s.uid);
    const dp = s.pts - (prev[s.uid] ?? 0);
    const dr = (rankPrev[s.uid] ?? s.rank) - s.rank;
    const medal = { 1: '🥇', 2: '🥈', 3: '🥉' }[s.rank] || `${s.rank}.`;
    const arrow = dr > 0 ? `<span class="up">▲${dr}</span>`
      : dr < 0 ? `<span class="down">▼${-dr}</span>` : `<span class="flat">■</span>`;
    return `<tr>
      <td class="rk">${medal}</td>
      <td style="color:${p.color};font-weight:600">${p.name}</td>
      <td class="ps">${p.pseudo}</td>
      <td class="ev">${arrow}</td>
      <td class="dp">+${dp.toLocaleString('fr-FR')}</td>
      <td class="tot">${s.pts.toLocaleString('fr-FR')}</td>
    </tr>`;
  }).join('');
  document.getElementById('app').insertAdjacentHTML('beforeend', `
    <section id="classement" class="card">
      <h2>🏆 Classement</h2>
      <div class="twrap"><table class="tbl">
        <thead><tr><th>#</th><th>Joueur</th><th>Pseudo</th><th>Évol</th><th>+pts</th><th>Total</th></tr></thead>
        <tbody>${rows}</tbody>
      </table></div>
    </section>`);
}
```
Then add `renderClassement();` in `initApp()` **before** the `#graphe` block (classement en premier après le podium).

- [ ] **Step 2: CSS du tableau**

Add in `<style>` :
```css
.twrap{overflow-x:auto}
.tbl{width:100%;border-collapse:collapse;font-size:15px}
.tbl th{color:var(--muted);text-align:left;font-weight:500;padding:8px 10px;border-bottom:1px solid #ffffff22}
.tbl td{padding:9px 10px;border-bottom:1px solid #ffffff10}
.tbl .rk{font-size:18px;text-align:center;width:44px}
.tbl .ps{color:var(--muted)}
.tbl .dp{text-align:right;color:var(--muted)}
.tbl .tot{text-align:right;font-weight:700;color:var(--gold)}
.tbl .ev{text-align:center}
.up{color:#38d996}.down{color:#ff6b6b}.flat{color:#6b739c}
```

- [ ] **Step 3: Rebuild et vérifier**

Run: `cd /home/willy/workspace/mppreporting && npm run build`
Verify (navigateur) : tableau 16 lignes, Christophe 1er (4095), JC 2e (4312 en J21→ vérifier deltas J21→J22 cohérents), flèches vertes/rouges, total en doré, scroll horizontal sur mobile.

- [ ] **Step 4: Commit**

```bash
git add src/app.js src/template.html dist/index.html
git commit -m "Page: section classement avec deltas et flèches d'évolution"
```

---

## Task 11: Section pronostics — 3 vues (grille / joueur / match)

**Files:**
- Modify: `src/app.js`, `src/template.html`

**Interfaces:**
- Consumes: `matches`, `forecasts`, `players`. Réimplémente localement `forecastStatus` et `playerStats` (mêmes règles qu'en Task 4 ; testées côté Node).
- Produces: section `#pronos` avec 3 sous-vues commutables (`data-view="grille|joueur|match"`), un `<select>` joueur et un `<select>` match. Pastilles statut : `exact` (vert), `result` (or), `miss` (rouge), `pending` (gris), chacune avec une lettre (E/R/✗/·) pour ne pas dépendre que de la couleur.

- [ ] **Step 1: Helpers pronos locaux**

Append to `src/app.js` :
```js
function outcome(a, b) { return a > b ? '1' : a < b ? '2' : 'N'; }
function fStatus(prono, m) {
  if (!prono || m.status !== 'played') return 'pending';
  if (prono.score1 === m.score1 && prono.score2 === m.score2) return 'exact';
  if (outcome(prono.score1, prono.score2) === outcome(m.score1, m.score2)) return 'result';
  return 'miss';
}
const PASTILLE = { exact: ['E', 'st-exact'], result: ['R', 'st-result'], miss: ['✗', 'st-miss'], pending: ['·', 'st-pending'] };
function pastille(st, label) {
  const [ch, cls] = PASTILLE[st];
  return `<span class="pill ${cls}" title="${label || ''}">${ch}</span>`;
}
function matchLabel(m) { return `${m.flag1} ${m.team1} ${m.score1 ?? '–'}-${m.score2 ?? '–'} ${m.team2} ${m.flag2}`; }
```

- [ ] **Step 2: Rendu des 3 vues**

Append to `src/app.js` :
```js
function renderPronosShell() {
  const { players, matches } = window.__WC;
  const playerOpts = players.map((p) => `<option value="${p.uid}">${p.name}</option>`).join('');
  const matchOpts = matches.map((m) => `<option value="${m.id}">${matchLabel(m)}</option>`).join('');
  document.getElementById('app').insertAdjacentHTML('beforeend', `
    <section id="pronos" class="card">
      <h2>🎯 Pronostics</h2>
      <div class="tabs">
        <button class="pv active" data-view="grille">Grille</button>
        <button class="pv" data-view="joueur">Par joueur</button>
        <button class="pv" data-view="match">Par match</button>
      </div>
      <div class="pv-controls">
        <select id="selPlayer" hidden>${playerOpts}</select>
        <select id="selMatch" hidden>${matchOpts}</select>
      </div>
      <div id="pronoBody"></div>
    </section>`);
  document.querySelectorAll('.pv').forEach((b) => b.addEventListener('click', () => {
    document.querySelectorAll('.pv').forEach((x) => x.classList.remove('active'));
    b.classList.add('active');
    showView(b.dataset.view);
  }));
  document.getElementById('selPlayer').addEventListener('change', () => showView('joueur'));
  document.getElementById('selMatch').addEventListener('change', () => showView('match'));
  showView('grille');
}

function showView(view) {
  document.getElementById('selPlayer').hidden = view !== 'joueur';
  document.getElementById('selMatch').hidden = view !== 'match';
  const body = document.getElementById('pronoBody');
  if (view === 'grille') body.innerHTML = viewGrille();
  else if (view === 'joueur') body.innerHTML = viewJoueur(document.getElementById('selPlayer').value);
  else body.innerHTML = viewMatch(document.getElementById('selMatch').value);
}

function viewGrille() {
  const { players, matches, forecasts } = window.__WC;
  const head = `<th>Match</th>` + players.map((p) => `<th class="vth" style="color:${p.color}">${p.name}</th>`).join('');
  const rows = matches.map((m) => {
    const cells = players.map((p) => {
      const pr = forecasts[p.uid]?.[m.id];
      const st = fStatus(pr, m);
      const lbl = pr ? `${pr.score1}-${pr.score2}` : '—';
      return `<td>${pastille(st, lbl)}</td>`;
    }).join('');
    return `<tr><td class="mcol">${matchLabel(m)}</td>${cells}</tr>`;
  }).join('');
  return `<div class="twrap"><table class="tbl grid"><thead><tr>${head}</tr></thead><tbody>${rows}</tbody></table></div>`;
}

function viewJoueur(uid) {
  const { matches, forecasts } = window.__WC;
  const p = window.__WC.players.find((x) => x.uid === uid);
  const pf = forecasts[uid] || {};
  let played = 0, exact = 0, resu = 0, pts = 0;
  const rows = matches.map((m) => {
    const pr = pf[m.id]; const st = fStatus(pr, m);
    if (st !== 'pending') { played++; if (st === 'exact') exact++; if (st === 'result') resu++; pts += pr?.points || 0; }
    return `<tr><td class="mcol">${matchLabel(m)}</td>
      <td>${pr ? `${pr.score1}-${pr.score2}` : '—'}</td>
      <td>${pastille(st)}</td><td class="dp">${pr?.points ?? ''}</td></tr>`;
  }).join('');
  const rate = played ? Math.round((exact / played) * 100) : 0;
  return `<div class="stats">
      <div class="stat"><b style="color:${p.color}">${p.name}</b><span>${p.pseudo}</span></div>
      <div class="stat"><b>${pts.toLocaleString('fr-FR')}</b><span>points pronos</span></div>
      <div class="stat"><b>${exact}</b><span>scores exacts</span></div>
      <div class="stat"><b>${rate}%</b><span>taux d'exacts</span></div>
    </div>
    <div class="twrap"><table class="tbl"><thead><tr><th>Match</th><th>Prono</th><th>Statut</th><th>Pts</th></tr></thead>
    <tbody>${rows}</tbody></table></div>`;
}

function viewMatch(id) {
  const { players, matches, forecasts } = window.__WC;
  const m = matches.find((x) => x.id === id);
  const rows = players.map((p) => {
    const pr = forecasts[p.uid]?.[id]; const st = fStatus(pr, m);
    return `<tr><td style="color:${p.color};font-weight:600">${p.name}</td>
      <td>${pr ? `${pr.score1}-${pr.score2}` : '—'}</td>
      <td>${pastille(st)}</td><td class="dp">${pr?.points ?? ''}</td></tr>`;
  }).join('');
  return `<p class="mhead">${matchLabel(m)} <span class="muted">(${m.phase})</span></p>
    <div class="twrap"><table class="tbl"><thead><tr><th>Joueur</th><th>Prono</th><th>Statut</th><th>Pts</th></tr></thead>
    <tbody>${rows}</tbody></table></div>`;
}
```
Then add `renderPronosShell();` in `initApp()` after `initChart();`.

- [ ] **Step 3: CSS pronos**

Add in `<style>` :
```css
.pv-controls{margin:6px 0 12px}
.pv-controls select{background:#141b3c;color:var(--ink);border:1px solid #ffffff33;border-radius:8px;padding:6px 10px}
.pill{display:inline-flex;align-items:center;justify-content:center;width:22px;height:22px;border-radius:6px;font-size:12px;font-weight:700;color:#0a0e27}
.st-exact{background:#38d996}.st-result{background:var(--gold)}.st-miss{background:#ff6b6b}
.st-pending{background:#3a4270;color:#aab}
.tbl.grid th.vth{writing-mode:vertical-rl;transform:rotate(180deg);height:70px;font-size:12px}
.tbl .mcol{white-space:nowrap;font-size:13px}
.stats{display:flex;gap:14px;flex-wrap:wrap;margin-bottom:14px}
.stat{background:#ffffff0d;border:1px solid #ffffff1a;border-radius:12px;padding:10px 14px;min-width:110px}
.stat b{display:block;font-size:20px}.stat span{color:var(--muted);font-size:12px}
.mhead{font-size:17px;font-weight:600}
```

- [ ] **Step 4: Rebuild et vérifier**

Run: `cd /home/willy/workspace/mppreporting && npm run build`
Verify (navigateur) : vue Grille (matchs × 2 joueurs des fixtures, pastilles E/R/✗/·) ; vue Par joueur (select → stats + tableau) ; vue Par match (select → pronos des joueurs). Bascule entre vues OK.

- [ ] **Step 5: Commit**

```bash
git add src/app.js src/template.html dist/index.html
git commit -m "Pronos: 3 vues (grille, par joueur, par match) avec pastilles de statut"
```

---

## Task 12: Fetch API MPP — token, standings, delta (+ endpoints pronos/fixtures)

> **Étape de découverte préalable (manuelle, une fois)** : connecté sur `https://mpp.football`, ouvrir les devtools (onglet Réseau), naviguer sur les pages « matchs » et « pronostics », repérer les requêtes `api.mpp.football/...` correspondantes, noter URL + forme de la réponse. Renseigner ces URLs dans les constantes `EP_MATCHES` / `EP_FORECASTS` de `fetch.mjs`. Le token se copie depuis `localStorage` (clé contenant `auth0spajs`, champ `body.access_token`) dans un fichier `.mpp-token` (gitignoré).

**Files:**
- Create: `src/fetch.mjs`, `src/fetch.test.mjs`

**Interfaces:**
- Consumes: token (`.mpp-token` ou `MPP_TOKEN`), API MPP.
- Produces:
  - `mergeHistory(existing, label, standings)` → nouvel objet history (idempotent : réécrit `label`).
  - `mergeById(existing, incoming, idKey)` → tableau fusionné (remplace les entrées de même id, ajoute les nouvelles).
  - `mergeForecasts(existing, incoming)` → deep-merge `{uid:{matchId:prono}}`.
  - `normalizeStandings(apiJson)` → `{ [uid]: points }` + `maxCalc`.
  - `main()` : lit le token, fetch, merge, écrit `data/*.json`.

- [ ] **Step 1: Tests des fonctions de merge (échouent)**

Create `src/fetch.test.mjs` :
```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { mergeHistory, mergeById, mergeForecasts, normalizeStandings } from './fetch.mjs';

test('mergeHistory ajoute/réécrit une journée (idempotent)', () => {
  const h0 = { J1: { a: 10 } };
  const h1 = mergeHistory(h0, 'J2', { a: 20, b: 5 });
  assert.deepEqual(h1.J2, { a: 20, b: 5 });
  const h2 = mergeHistory(h1, 'J2', { a: 21, b: 5 }); // rejoue J2
  assert.deepEqual(h2.J2, { a: 21, b: 5 });
  assert.equal(Object.keys(h2).length, 2);
});

test('mergeById remplace par id et ajoute les nouveaux', () => {
  const cur = [{ id: 'a', v: 1 }, { id: 'b', v: 2 }];
  const inc = [{ id: 'b', v: 9 }, { id: 'c', v: 3 }];
  assert.deepEqual(mergeById(cur, inc, 'id'), [{ id: 'a', v: 1 }, { id: 'b', v: 9 }, { id: 'c', v: 3 }]);
});

test('mergeForecasts fusionne par joueur puis par match', () => {
  const cur = { u: { m1: { score1: 1, score2: 0, points: 16 } } };
  const inc = { u: { m2: { score1: 2, score2: 2, points: 0 } }, v: { m1: { score1: 0, score2: 0, points: 8 } } };
  const out = mergeForecasts(cur, inc);
  assert.equal(out.u.m1.points, 16);
  assert.equal(out.u.m2.score1, 2);
  assert.equal(out.v.m1.points, 8);
});

test('normalizeStandings extrait uid→points et maxCalc', () => {
  const api = { standings: [
    { user: { id: 'user_1' }, ranking: { points: 100, calculatedForecasts: 5 } },
    { user: { id: 'user_2' }, ranking: { points: 80, calculatedForecasts: 4 } },
  ] };
  const { points, maxCalc } = normalizeStandings(api);
  assert.deepEqual(points, { user_1: 100, user_2: 80 });
  assert.equal(maxCalc, 5);
});
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cd /home/willy/workspace/mppreporting && node --test src/fetch.test.mjs`
Expected: FAIL — `Cannot find module './fetch.mjs'`.

- [ ] **Step 3: Implémenter fetch.mjs**

Create `src/fetch.mjs` :
```js
import { readFileSync, writeFileSync, existsSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..');
const CHALLENGE = 'mpp_challenge_UDMSMC8T';
const API = 'https://api.mpp.football';
const EP_STANDINGS = `${API}/challenge-standings/users-standings?challengeId=${CHALLENGE}&offset=0&limit=20`;
// À renseigner après découverte devtools (cf. étape préalable) :
const EP_MATCHES = null;   // ex: `${API}/...`
const EP_FORECASTS = null; // ex: `${API}/...`

const jpath = (p) => join(ROOT, p);
const readJson = (p) => JSON.parse(readFileSync(jpath(p), 'utf8'));
const writeJson = (p, o) => writeFileSync(jpath(p), JSON.stringify(o, null, 2) + '\n');

export function normalizeStandings(api) {
  const points = {}; let maxCalc = 0;
  for (const s of api.standings || []) {
    points[s.user.id] = s.ranking.points;
    maxCalc = Math.max(maxCalc, s.ranking.calculatedForecasts ?? 0);
  }
  return { points, maxCalc };
}
export function mergeHistory(existing, label, standings) {
  return { ...existing, [label]: { ...standings } };
}
export function mergeById(existing, incoming, idKey) {
  const map = new Map(existing.map((e) => [e[idKey], e]));
  for (const it of incoming) map.set(it[idKey], { ...map.get(it[idKey]), ...it });
  return [...map.values()];
}
export function mergeForecasts(existing, incoming) {
  const out = { ...existing };
  for (const [uid, byMatch] of Object.entries(incoming)) out[uid] = { ...out[uid], ...byMatch };
  return out;
}

function token() {
  if (process.env.MPP_TOKEN) return process.env.MPP_TOKEN.trim();
  if (existsSync(jpath('.mpp-token'))) return readFileSync(jpath('.mpp-token'), 'utf8').trim();
  throw new Error('Token MPP absent : renseigner .mpp-token ou MPP_TOKEN (voir CLAUDE.md).');
}
async function get(url, tok) {
  const r = await fetch(url, { headers: { Authorization: `Bearer ${tok}` } });
  if (r.status === 401) throw new Error('401 : token MPP expiré, le rafraîchir.');
  if (!r.ok) throw new Error(`${r.status} sur ${url}`);
  return r.json();
}

async function main() {
  const tok = token();
  const nextLabel = process.argv[2] || `J${Object.keys(readJson('data/history.json')).length + 1}`;

  const std = normalizeStandings(await get(EP_STANDINGS, tok));
  writeJson('data/history.json', mergeHistory(readJson('data/history.json'), nextLabel, std.points));
  console.log(`OK  history ${nextLabel} (maxCalc=${std.maxCalc})`);

  if (EP_MATCHES) {
    const raw = await get(EP_MATCHES, tok);
    // TODO découverte : mapper raw → [{id,phase,date,team1,flag1,team2,flag2,score1,score2,status}]
    // writeJson('data/matches.json', mergeById(readJson('data/matches.json'), mapped, 'id'));
  } else console.log('… EP_MATCHES non renseigné (voir étape de découverte)');

  if (EP_FORECASTS) {
    const raw = await get(EP_FORECASTS, tok);
    // TODO découverte : mapper raw → {uid:{matchId:{score1,score2,points,result}}}
    // writeJson('data/forecasts.json', mergeForecasts(readJson('data/forecasts.json'), mapped));
  } else console.log('… EP_FORECASTS non renseigné (voir étape de découverte)');
}

if (import.meta.url === `file://${process.argv[1]}`) main().catch((e) => { console.error(e.message); process.exit(1); });
```
> Les blocs `EP_MATCHES` / `EP_FORECASTS` et leur mapping sont complétés une fois les endpoints découverts. Tant qu'ils sont `null`, `fetch.mjs` met à jour uniquement `history.json` (standings, endpoint confirmé) — le reste vient des fixtures Task 5 jusqu'à la découverte.

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `cd /home/willy/workspace/mppreporting && node --test src/fetch.test.mjs`
Expected: PASS (4 tests).

- [ ] **Step 5: Test d'intégration standings (nécessite un token valide)**

Prérequis : `.mpp-token` renseigné.
Run: `cd /home/willy/workspace/mppreporting && node src/fetch.mjs J23`
Expected: `OK  history J23 (maxCalc=…)` et `data/history.json` contient `J23`.
Si pas de token disponible au moment de l'exécution : noter l'étape comme « à rejouer avec token » et continuer (les tests unitaires suffisent à valider la logique).

- [ ] **Step 6: Commit**

```bash
git add src/fetch.mjs src/fetch.test.mjs
git commit -m "Fetch: standings + logique de merge delta (matchs/pronos après découverte endpoints)"
```

---

## Task 13: Section bilan + finalisation (docs, déploiement)

**Files:**
- Modify: `src/app.js`, `src/template.html`, `CLAUDE.md`
- Create: `data/bilan.json`

**Interfaces:**
- Consumes: `window.__WC.bilan` (texte HTML d'analyse). `build.mjs` doit inclure `bilan` dans les données injectées.
- Produces: section `#bilan` rendant le HTML du bilan ; workflow documenté dans `CLAUDE.md`.

- [ ] **Step 1: Créer data/bilan.json (texte repris de l'existant)**

Create `data/bilan.json` :
```json
{
  "updated": "J22 — 8 juillet 2026",
  "html": "<p>Le miracle argentin, la Suisse qui s'endort, et surtout : <b>JC</b> et <b>Manu</b> qui prennent 0 pt sur le cycle. <b>Kawett</b> rafle la mise (+156) et grimpe sur le podium. Rendez-vous demain pour France-Maroc… arbitré par un trio 100% argentin. Le dossier est ouvert.</p>"
}
```

- [ ] **Step 2: Injecter bilan dans le build**

In `src/build.mjs`, `build()` : add `bilan: readJson('data/bilan.json')` to the object passed to `buildHtml`, and in `buildHtml` include it in `data` : change the `data` line to
```js
  const data = JSON.stringify({ players, history, matches, forecasts, bilan });
```
and add `bilan` to the `buildHtml` destructured params.

- [ ] **Step 3: Mettre à jour le test de build**

In `src/build.test.mjs`, add `bilan: { updated: 'J1', html: '<p>Salut</p>' }` to the `buildHtml` call and assert `assert.match(html, /Salut/);`.
Run: `cd /home/willy/workspace/mppreporting && node --test src/build.test.mjs`
Expected: PASS.

- [ ] **Step 4: Rendre la section bilan**

Append to `src/app.js` :
```js
function renderBilan() {
  const b = window.__WC.bilan || { html: '', updated: '' };
  document.getElementById('app').insertAdjacentHTML('beforeend', `
    <section id="bilan" class="card">
      <h2>🔍 Le mot du bilan <span class="muted">${b.updated}</span></h2>
      <div class="prose">${b.html}</div>
    </section>
    <footer class="foot">SHRS Football Club — Coupe du Monde 2026 • généré statiquement</footer>`);
}
```
Then add `renderBilan();` at the end of `initApp()`.

- [ ] **Step 5: CSS bilan**

Add in `<style>` :
```css
.prose{line-height:1.7;font-size:16px;color:#dfe4ff}
.prose b{color:#fff}
.foot{text-align:center;color:var(--muted);font-size:12px;padding:26px 0}
```

- [ ] **Step 6: Rebuild et vérifier l'ensemble**

Run: `cd /home/willy/workspace/mppreporting && npm run build && node --test`
Expected: build OK + **tous** les tests PASS (compute + fetch + build).
Verify (navigateur) : la page complète défile — hero, podium, classement, graphe interactif + replay, pronos 3 vues, bilan, footer. Responsive (réduire la fenêtre).

- [ ] **Step 7: Documenter le workflow dans CLAUDE.md**

Add a section at the top of `CLAUDE.md` :
```markdown
## Page statique (nouveau livrable — remplace l'email)

Workflow de mise à jour à chaque journée :

1. Rafraîchir le token si besoin : copier `body.access_token` depuis le localStorage
   (clé `auth0spajs`) de https://mpp.football dans `.mpp-token` (gitignoré).
2. `node src/fetch.mjs J<N>` — récupère standings (+ matchs/pronos si endpoints renseignés),
   met à jour `data/*.json` en delta.
3. Mettre à jour `data/bilan.json` (le mot du jour, ton chambreur).
4. `npm run build` — régénère `dist/index.html` (auto-suffisant).
5. `npm test` — vérifie la logique.
6. Commit `data/*.json` + `dist/index.html`.
7. Déployer `dist/index.html` sur Cloudflare Pages (glisser-déposer le dossier `dist/`
   dans le dashboard, ou repo Git connecté avec build command vide et output `dist`).

Endpoints matchs/pronos : à découvrir une fois via devtools réseau sur mpp.football,
puis renseigner `EP_MATCHES` / `EP_FORECASTS` dans `src/fetch.mjs`.
```

- [ ] **Step 8: Commit final**

```bash
git add src/app.js src/build.mjs src/build.test.mjs src/template.html data/bilan.json CLAUDE.md dist/index.html
git commit -m "Bilan: section éditoriale, workflow documenté, page complète"
```

---

## Notes d'exécution

- **Ordre** : Tasks 1→6 posent les fondations (données + build + logique testée). Tasks 7→11+13 sont le visuel (invoquer **frontend-design** et **dataviz** au fil de l'eau). Task 12 (fetch réel) peut être faite dès que le token + les endpoints sont dispo ; jusque-là, les fixtures (Task 5) permettent de tout construire et vérifier.
- **Vérification visuelle** : `dist/index.html` étant auto-suffisant, il s'ouvre directement (double-clic / `file://`) sans serveur.
- **Découverte endpoints** : seul vrai inconnu ; cerné à la Task 12. Le reste ne dépend pas d'elle.
```
